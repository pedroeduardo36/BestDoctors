import React from 'react';
import {BaseBackground, Title} from '~/components';

export const ProfileScreen = () => {
    return (
        <BaseBackground>
            <Title>Perfil</Title>
        </BaseBackground>
    );
};
